# Models

Place SAM2 checkpoints and any other model weights in this folder.

Suggested layout:
- `models/sam2/` for SAM2 checkpoints and configs
- `models/yolo/` for trained YOLO weights

This repo does not auto-download large weights. Download them manually and place here.
